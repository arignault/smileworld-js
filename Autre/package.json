{
    "name": "smile-world-gsap",
    "version": "1.0.0",
    "description": "",
    "main": "index-js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "live-server --port=8080 --host=127.0.0.1 --entry-file=www/index.html --cors --no-browser --middleware=./middleware.js"
    },
    "author": "Alexandre Rignault",
    "License": "ISC",
    "devDependencies": {
        "http-server": "^14.1.1",
        "live-server": "^1.2.2",
        "mkcert": "^3.2.0"
    }
}
